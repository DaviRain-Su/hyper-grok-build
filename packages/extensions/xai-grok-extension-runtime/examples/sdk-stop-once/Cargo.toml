[workspace]

[package]
name = "sdk-stop-once"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]
xai-grok-extension-sdk = { path = "../../../xai-grok-extension-sdk" }

[profile.release]
opt-level = "s"
lto = true
strip = true
